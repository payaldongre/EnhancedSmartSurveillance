# =============================================================================
# EnhancedSmartSurveillance — full runtime requirements (Python 3.10).
#
# One-shot install:   pip install -r requirements.txt
#
# These defaults are self-contained: they include the CPU build of PyTorch, so
# the app runs on any machine with no extra steps. For NVIDIA GPU acceleration,
# replace the three torch lines with the CUDA wheels (see README step 4).
# =============================================================================

# Backend
flask==2.3.3

# -----------------------------------------------------------------------------
# Computer Vision
#
# IMPORTANT: only ONE OpenCV distribution may be installed. mediapipe depends on
# opencv-contrib-python, and easyocr (optional, below) depends on
# opencv-python-headless. Installing more than one leaves them fighting over the
# same `cv2` folder, which breaks imports with:
#     AttributeError: module 'cv2' has no attribute 'setNumThreads'
# opencv-contrib-python is a superset of opencv-python, so it covers everything.
# -----------------------------------------------------------------------------
opencv-contrib-python==4.8.1.78
Pillow==10.0.1

# Deep Learning - YOLOv8 (Ultralytics) + PyTorch (CPU build by default)
ultralytics==8.0.196
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# Pose Estimation
mediapipe==0.10.7

# Numerical / ML
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0
python-dateutil==2.8.2

# Visualization
matplotlib==3.7.2
seaborn==0.12.2

# Advanced tracking helpers
filterpy==1.4.5
scikit-image==0.20.0

# Optional MQTT transport for the C2 integration (the HTTP webhook needs nothing).
paho-mqtt==2.1.0

# -----------------------------------------------------------------------------
# OPTIONAL extras — NOT installed by default.
#
# ANPR (number-plate OCR) needs ONE OCR backend. easyocr reuses the torch
# install and needs no system binary (recommended). It pulls in
# opencv-python-headless, which clashes with opencv-contrib-python, so install
# it and then restore the single OpenCV build:
#
#     pip install easyocr "numpy<2"
#     pip uninstall -y opencv-python-headless opencv-contrib-python
#     pip install "opencv-contrib-python==4.8.1.78"
#
# Lighter alternative (needs the Tesseract binary on PATH):
#     pip install pytesseract==0.3.13
# -----------------------------------------------------------------------------
