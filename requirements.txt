# Backend
flask==2.3.3

# Computer Vision
opencv-python==4.8.1.78
Pillow==10.0.1

# Deep Learning - YOLOv8 (Ultralytics) + PyTorch
ultralytics==8.0.196
# torch / torchvision / torchaudio are intentionally NOT installed via this file —
# the correct wheel depends on your hardware (NVIDIA GPU + CUDA version, or
# CPU-only). See README.md step 4 for the exact install command for your machine.
# torch==2.5.1
# torchvision==0.20.1
# torchaudio==2.5.1

# Pose Estimation
mediapipe==0.10.7

# Numerical / ML
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0
python-dateutil==2.8.2

# Visualization
matplotlib==3.7.2
seaborn==0.12.2

# tensorflow  # or a TF version compatible with your environment; install only if you will train/load Keras models

# (optional) for advanced tracking later:
filterpy==1.4.5
scikit-image==0.20.0

# ---------------------------------------------------------------------------
# OPTIONAL extras for the added capabilities. The default install above already
# covers vehicle detection/classification, face detection, virtual fencing and
# night-time detection (they use OpenCV + the existing models only).
# ---------------------------------------------------------------------------

# ANPR (number-plate recognition) needs ONE OCR backend. easyocr reuses the
# torch install from step 4 and needs no extra system binary — recommended.
# easyocr==1.7.1
# (alternative: lighter Python side, but requires the Tesseract OCR binary on PATH)
# pytesseract==0.3.13

# MQTT transport for the Command & Control integration (webhook needs nothing).
# paho-mqtt==2.1.0
